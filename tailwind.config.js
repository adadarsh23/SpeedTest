/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // scans all React components inside src folder
  ],
  darkMode: 'class', // Enables dark mode via `class="dark"`
  theme: {
    extend: {},
  },
  plugins: [],
};
